# Dependencies
node_modules/

# نسخة أوفلاين من صوت القرآن (opus 10kbps، islamic-app/audio/) تُنشر عبر
# GitHub Pages للتنزيل الاختياري — مستبعدة من www/ (ومن ثم الـ APK وحزمة
# التحديث الحي) في scripts/sync-web.js لأن البث الافتراضي من everyayah.com
www/audio/

# APK المفكوك (مرجع محلي فقط) — والإصدارات تُوزَّع من GitHub Releases
# لا من المستودع، فأي APK في الجذر لا يُتتبَّع
base/
*.apk

# Android build output
android/build/
android/app/build/
android/.gradle/
android/local.properties
android/capacitor-cordova-android-plugins/build/
*.keystore
*.jks

# أدوات المحرر
.claude/
.vscode/
.idea/
.DS_Store
Thumbs.db
